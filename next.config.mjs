/** @type {import('next').NextConfig} */
export default {
    images: {
      domains: ['drive.google.com'],
    },
    reactStrictMode: true,
    experimental: {
      serverActions: true
    }
  };
