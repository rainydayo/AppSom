
// This cannot be used. But keep it for now //

import { writeFile } from "fs";
import { Board } from "../../interface";
import GetBoards from "./GetBoards";

export default async function CreateBoard (board: Board) {
    // const boards = await GetBoards();
    // var obj = JSON.parse(boards);
    // obj.data.push(board);
    // var updated = JSON.stringify(obj);
    // writeFile('board.json', updated, 'utf8', () => {
        
    // });
}